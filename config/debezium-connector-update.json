{
  "connector.class": "io.debezium.connector.postgresql.PostgresConnector",
  "database.hostname": "haproxy",
  "database.port": "5000",
  "database.user": "replicator",
  "database.password": "repl_pass_2024",
  "database.dbname": "sasdatqbox",
  "plugin.name": "pgoutput",
  "table.include.list": "pos.outbox",
  "topic.prefix": "dbz-outbox",
  "publication.autocreate.mode": "disabled",
  "publication.name": "dbz_outbox_pub",
  "slot.name": "debezium_outbox_slot",
  "transforms": "outbox",
  "transforms.outbox.type": "io.debezium.transforms.outbox.EventRouter",
  "transforms.outbox.route.by.field": "type",
  "transforms.outbox.route.topic.replacement": "${routedTopic}",
  "transforms.outbox.table.field.event.key": "aggregate_id",
  "transforms.outbox.table.field.event.payload": "payload",
  "transforms.outbox.additional.fields": "eventType:type,aggregateId:aggregate_id"
}